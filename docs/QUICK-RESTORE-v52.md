# 🔄 استرجاع سريع للنسخة v52

## 🚨 للطوارئ - استرجاع فوري

```bash
# العودة للنسخة المستقرة v52
git checkout main
git reset --hard v52-stable
git push origin main --force

# تأكيد النسخة
git log --oneline -1
# يجب أن تشاهد: v52-stable
```

## 📋 معلومات النسخة السريعة

| المعلومة | القيمة |
|---------|--------|
| **Git Tag** | `v52-stable` |
| **Commit Hash** | `f945f47` |
| **شارة النسخة** | `v52 ✅` (أخضر-أزرق) |
| **عدد الروابط في Sitemap** | 34 رابط |
| **حالة GitHub** | نظيف - main only |
| **حالة Vercel** | Production Ready |

## ✅ التحقق من سلامة النسخة

```bash
# فحص الملفات الرئيسية
ls -la public/sitemap.xml
grep "v52" src/components/Header.jsx
cat vercel.json | grep -A5 "rewrites"

# فحص عدد الروابط في sitemap
grep -c "<url>" public/sitemap.xml
# يجب أن يكون: 17 (34 رابط ÷ 2)
```

## 🔧 أوامر الصيانة

```bash
# تنظيف فروع محلية قديمة
git branch | grep -v main | xargs git branch -D

# تحديث مراجع بعيدة
git fetch origin --prune

# فحص حالة المستودع
git status
git log --oneline -5
```

## 📞 معلومات مهمة

- **لا تحذف** الـ tag `v52-stable` أبداً
- **اختبر دائماً** قبل الدمج في main
- **ابدأ من v52-stable** لأي تطوير جديد
- **احفظ نسخ احتياطية** قبل التحديثات الكبيرة

---
**تم إنشاؤه في**: 2025-08-28  
**آخر تحديث**: اليوم  
**الحالة**: مستقر ✅