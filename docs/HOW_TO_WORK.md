# 📚 دليل العمل الصحيح - VägVänner

## ⚠️ قاعدة ذهبية:
**اعمل على main فقط - لا تنشئ فروع جديدة!**

## 🎯 خطوات العمل الصحيحة:

### 1. قبل البدء:
```bash
git checkout main
git pull origin main
```

### 2. اعمل تغييراتك:
- عدّل الملفات
- أضف ميزات
- أصلح الأخطاء

### 3. احفظ وانشر:
```bash
git add -A
git commit -m "وصف التغيير"
git push origin main
```

### 4. Vercel ينشر تلقائياً:
- انتظر 2-3 دقائق
- تحقق من vagvanner.se

## ❌ لا تفعل:
- لا تنشئ فروع جديدة
- لا تستخدم `git checkout -b`
- لا تنشئ pull requests

## ✅ افعل:
- اعمل على main مباشرة
- احفظ تغييراتك بانتظام
- اكتب رسائل commit واضحة

## 🔧 إذا حدثت مشكلة:
```bash
# ارجع آخر commit
git revert HEAD

# أو ارجع لحالة سابقة
git reset --hard HEAD~1
```

## 📊 للتحقق من الحالة:
```bash
git status
git log --oneline -5
```

---

**تذكر:** main = الإنتاج = vagvanner.se

كل push إلى main ينشر مباشرة!