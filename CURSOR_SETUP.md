# تعليمات للوكلاء الجدد
# Instructions for New AI Agents

هذا المستودع يحتوي على النسخة المستقرة في فرع `main`.

## الـ Commit الصحيح:
`c01a3408 دمج النسخة المستقرة من cursor/send-a-greeting-8830`

## عند بدء جلسة جديدة:
```bash
git checkout main
git pull origin main
```

## للتحقق من النسخة:
```bash
git log --oneline -1
```

يجب أن ترى: c01a3408

## هذه هي النسخة المستقرة والمختبرة التي تعمل بدون أخطاء.